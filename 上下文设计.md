# 上下文设计

本文档记录 Codepilot 的 5 层上下文分层、压力感知方式，以及不同压力下的裁剪/压缩原则。整体原则是：System、Task State、Memory 尽量稳定；Working Set 通过准入控制大小；Conversation 是主要压缩对象。

## Agent-loop 中的上下文流程

每次进入 agent-loop 时，上下文治理器按下面的流程工作：

```text
1. 更新 Context Store
   保存用户消息、助手回复、工具调用、工具结果、任务状态、仓库观察和记忆信息。

2. 按 5 层组织上下文
   将信息整理为 System、Task State、Working Set、Memory、Conversation。

3. 生成默认 Prompt Context
   System 常驻；Task State 使用结构化摘要；Working Set 只放当前任务需要的工作集；
   Memory 只放少量相关记忆；Conversation 保留最近交流和短期工具上下文。

4. 感知上下文压力
   估算默认 Prompt Context 会占用多少 token，并和当前模型可用输入预算比较。

5. 优先处理 Conversation Context
   如果压力较高，优先压缩旧对话、旧工具结果和长日志；保留最近 tail 原文。

6. 必要时限制 Working Set
   如果处理 Conversation 后仍然超预算，再收缩 Working Set 的代码片段和弱相关信息。

7. 生成最终 Prompt Context
   组装真正发送给模型的上下文。
```

## 上下文压力感知

压力感知用于判断默认 Prompt Context 是否已经接近模型可用输入上限。第一版先分 3 个等级，并由压力等级触发下方定义的裁剪/压缩原则。

### 1. 计算可用输入预算

可用输入预算不是模型完整上下文窗口，而是扣除输出预留和安全缓冲后的输入空间：

```text
usable_input_budget =
  model_context_limit
  - reserved_output_tokens
  - safety_buffer
```

其中：

- `model_context_limit`：当前模型的上下文窗口上限。
- `reserved_output_tokens`：给本轮模型回复预留的 token。
- `safety_buffer`：防止估算误差、消息包装开销、工具 schema 波动导致超窗。

### 2. 估算默认 Prompt Context

每次 agent-loop 先按 5 层生成默认 Prompt Context，再估算它的 token：

```text
raw_prompt_tokens =
  estimate(system_context)
  + estimate(task_state_context)
  + estimate(working_set_context)
  + estimate(memory_context)
  + estimate(conversation_context)
  + estimate(latest_user_message)
  + estimate(tool_schema)

message_overhead_tokens =
  message_count * 12
  + tool_call_count * 20

uncalibrated_prompt_tokens =
  raw_prompt_tokens + message_overhead_tokens

estimated_prompt_tokens =
  uncalibrated_prompt_tokens * correction_factor
```

第一版使用轻量 token 估算器，不直接使用单一的 `text_length / 4`。估算器根据内容类型选择不同系数：

```text
estimate(item):
  code_text:      round(text_length / 3.5)
  english_text:   round(text_length / 4)
  chinese_text:   round(text_length / 1.8)
  json_schema:    round(text_length / 3)
  tool_output:    round(text_length / 3)
  markdown_mixed: round(text_length / 2.5)
  default:        round(text_length / 4)
```

其中：

- `message_count * 12` 用于估算消息包装开销。
- `tool_call_count * 20` 用于估算工具调用结构开销。
- `correction_factor` 初始值为 `1.0`，后续根据真实 usage 反馈校准。

后续如果接入模型 tokenizer，可以替换轻量估算器，在最终发送前进行更精确计算。

### 3. 计算压力比例

```text
pressure_ratio = estimated_prompt_tokens / usable_input_budget
```

压力等级先分 3 档：

```text
normal:   pressure_ratio < 0.70
high:     0.70 <= pressure_ratio < 0.90
critical: pressure_ratio >= 0.90
```

压力等级只负责判断当前上下文是否紧张；具体处理方式见下方“上下文裁剪/压缩策略”。

### 4. 使用 usage 反馈校准

每次模型调用结束后，如果模型返回真实 token 使用量，就记录估算误差：

```text
estimate_error = actual_input_tokens - estimated_prompt_tokens
current_factor = actual_input_tokens / uncalibrated_prompt_tokens
```

后续通过滑动平均更新校准系数：

```text
correction_factor = moving_average(previous_factor, current_factor)
```

为了避免单次异常影响太大，`correction_factor` 不应只取最近一次调用结果。

## 上下文裁剪/压缩策略

### 总体原则

不同上下文层采用不同策略：

```text
System Context:
  常驻，不做运行时裁剪。如果太大，应优化系统提示词本身。

Task State Context:
  常驻，保持结构化短摘要。不做常规压缩。

Memory Context:
  尽量稳定。依赖记忆写入和检索阶段控制质量，进入 prompt 的记忆默认都是重要信息。

Working Set Context:
  以准入控制为主，只放当前任务需要的工作集。极端压力下再收缩。

Conversation Context:
  主要压缩对象。随着压力升高，从原文保留逐步降级到确定性摘要，再到 LLM compact。
```

### normal：低压力

`normal` 阶段尽量保留完整上下文体验。

处理方式：

- System、Task State、Memory 原样保留。
- Working Set 保留当前 active files、必要代码片段、局部仓库摘要和当前任务需要的 skill 信息。
- Conversation 中的自然语言对话可以完整保留。
- 最近工具结果可以保留原文或摘录。
- 工具结果仍需要基础硬限制，避免单次大输出撑爆上下文。

工具结果的基础硬限制包括：

- 超长命令输出截断。
- 大文件读取结果不无限进入 Conversation。
- 二进制、base64、图片等只放描述。
- 完整原始结果保存在 Context Store，Prompt Context 只放可读版本。

### high：高压力

`high` 阶段不调用 LLM 做 compact，优先使用确定性规则降低 Conversation 体积。

处理方式：

- System、Task State、Memory 原样保留。
- Conversation 保留最近 tail 原文。
- 旧工具结果转为规则摘要或占位。
- 旧长日志只保留退出码、关键错误行、关键命令和少量上下文。
- 旧的普通工具成功结果可以只保留摘要或占位。
- Working Set 停止扩展弱相关信息，只保留 active files、当前编辑片段、错误相关片段和必要摘要。

示例：

```text
[tool result omitted: old, 18320 chars, command=pytest, status=passed]
[tool result summarized: failed at tests/test_context.py:42, AssertionError: active_goal missing]
```

### critical：临界压力

`critical` 阶段才使用 LLM compact，目标是让会话能够继续。

处理方式：

- System、Task State、Memory 原样保留。
- Conversation 切分为 `head` 和 `tail`。
- `head` 是较旧历史，交给 LLM 生成结构化摘要。
- `tail` 是最近若干轮，原文保留，保证对话连续性。
- 旧工具结果进入 compact prompt 前也应先用截断/摘要版本，避免把大日志原样交给 LLM。
- compact 后的关键目标、约束、当前状态和下一步应同步回 Task State。
- 如果 critical 后仍超预算，Working Set 收缩到最小可继续执行集合。

compact 摘要建议包含：

```text
## Objective
- 用户要完成什么

## Important Details
- 用户约束、偏好、已确认决策、关键事实

## Work State
- Completed: 已完成事项
- Active: 当前正在做的事
- Blocked: 阻塞、失败命令或未知问题

## Next Move
1. 立即下一步
2. 后续动作
```

## 5 层上下文

### 1. System Context：系统约束

存放稳定规则和硬约束。

应收集：

- agent 的角色定位和工作原则。
- 工具调用规则。
- 安全规则和权限边界。
- 项目级硬约束。
- 输出格式和编码规范。

不放动态仓库信息，例如当前文件列表、代码片段、git diff、工具输出。

### 2. Task State Context：任务状态

存放当前任务的权威状态。

应收集：

- 当前任务目标。
- 当前用户原话需求。
- 用户明确要求和禁止事项。
- 当前工作姿态，例如 discuss、explore、act、recover。
- 当前计划、当前步骤、已完成事项。
- 已确认决策。
- 未解决问题。
- 关键证据摘要。
- 验证状态、风险和未完成事项。

这层用于保证压缩后仍能恢复任务现场。

### 3. Working Set Context：工作集

存放当前任务相关的仓库、文件、代码片段和按需能力信息。Working Set 不是全量仓库知识库，而是“工作索引 + 少量精确材料”。

应收集：

- Active Files：当前正在编辑、刚读取、用户显式引用或任务正在围绕处理的文件。
- Exact Snippets：当前推理、修改或调试必须精确看到的代码片段。
- Repo Signals：仓库结构局部摘要、git status、git diff 摘要、测试入口和相关模块路径。
- Search Results：当前任务相关搜索结果摘要和关键命中。
- Symbol Context：相关函数、类、接口、调用关系。
- Skill Info：当前任务按需加载的 skill 名称、用途和必要规则摘要。

这层不保存全量仓库信息，只保存当前任务需要的工作集。Working Set 需要认真控制准入：

- 优先放 active files、当前编辑片段、错误相关片段。
- 大文件默认只放相关函数、类或摘要。
- 仓库结构只放当前任务需要的局部摘要。
- 工具长输出和历史对话不放在这里。
- skill 只有在当前任务需要时才加入，不应把所有 skill 全量放入上下文。

信息进入 Working Set 至少应满足一个条件：

- 用户显式引用。
- agent 当前正在编辑。
- 最近工具读取后被标记为 active。
- 与当前错误、测试、修改目标直接相关。
- 是当前任务必需的 skill 规则。

### 4. Memory Context：记忆信息

存放跨任务、跨会话仍然有价值的信息。

应收集：

- 用户长期偏好。
- 项目长期惯例。
- 历史设计决策。
- 过去任务总结出的经验。
- 经常出现且仍有效的限制。

记忆不能覆盖用户当前最新指令。

### 5. Conversation Context：交流信息

存放最近几轮对话和短期工具上下文。

应收集：

- 最近若干轮用户原文。
- 最近若干轮助手回复。
- 最近关键工具调用。
- 最近关键工具结果或结果摘录。
- 当前回合刚产生、还没整理进 Task State 的信息。

旧工具结果不应长期保留在这里；关键事实应提炼进 Task State，普通旧结果后续可替换为引用或占位。

## 示例

用户说：“帮我设计上下文治理，先不要读代码，我们先讨论分层。”

一次 agent-loop 中可以收集为：

```text
System Context:
- Codepilot 是面向学生学习与求职展示的本地 Coding Agent 项目。
- 优先保持代码和设计清晰、可解释、可演示。
- 中文文档使用 UTF-8。

Task State Context:
- goal: 设计 Codepilot 的上下文治理。
- raw_user_request: “帮我设计上下文治理，先不要读代码，我们先讨论分层。”
- user_constraints: 暂时不要读代码，先讨论设计。
- current_mode: discuss。
- current_step: 定义上下文分层。
- decisions: 使用 5 层上下文。
- open_questions: 后续实现 Context Store、Prompt Builder、Token Estimator 和 Conversation Compactor。

Working Set Context:
- active_files: 上下文设计.md。
- files_in_scope: 当前设计文档。
- repo_summary: 本项目是本地 Coding Agent 项目，强调清晰和可演示。
- active_skill: system-architect，用于约束上下文治理的架构设计。

Memory Context:
- 用户偏好先讨论设计，再落代码。
- 用户关注 Claude Code、OpenCode、Harness 等 coding agent 的设计细节。

Conversation Context:
- 用户希望先写出 5 层上下文分别是什么、应该收集什么信息，并举例说明。
- 助手准备把文档精简为流程、分层和示例。
```

本文档先完成上下文分层、压力感知、裁剪/压缩原则设计。后续实现时需要把这些规则落到 Context Store、Prompt Builder、Token Estimator 和 Conversation Compactor 中。
