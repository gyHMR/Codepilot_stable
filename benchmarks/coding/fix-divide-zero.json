{
  "id": "fix-divide-zero",
  "domain": "coding",
  "fixture": "calculator",
  "prompt": "修复 calculator.py 中 divide() 的除零行为：除数为 0 时抛出 ValueError，并运行测试。",
  "budgets": {
    "timeout_seconds": 120,
    "max_tool_calls": 12
  },
  "assertions": [
    {
      "type": "command",
      "command": "python -m pytest -q",
      "expect_exit_code": 0
    },
    {
      "type": "diff",
      "allowed_paths": ["calculator.py"],
      "forbidden_paths": ["test/"]
    }
  ]
}
