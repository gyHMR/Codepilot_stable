{
  "id": "fix-clamp-boundaries",
  "domain": "coding",
  "fixture": "clamp",
  "prompt": "修复 clamp.py 的边界处理，使小于下界返回下界、大于上界返回上界，并运行测试。不要修改测试。",
  "budgets": {
    "timeout_seconds": 120,
    "max_tool_calls": 12
  },
  "assertions": [
    {
      "type": "command",
      "command": "python -m pytest -q",
      "expect_exit_code": 0
    },
    {
      "type": "diff",
      "allowed_paths": ["clamp.py"],
      "forbidden_paths": ["test/"]
    }
  ]
}
