# Context Heavy Fixture

这个 fixture 用于触发上下文治理评估。它包含多份项目说明、历史约束和容易混淆的旧信息，目的是观察 Agent 在准备上下文时能否保留当前请求、控制 token，并避免把过期或无关信息当成当前事实。

当前项目目标：

- 提供一个本地可靠性评估样例。
- 使用 README.md 作为当前任务入口。
- 使用 docs/architecture.md 作为主要架构说明。
- 不需要修改任何文件。

