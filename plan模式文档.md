# Agent 运行模式设计说明

## 1. 设计目标

本项目支持 `Build`、`Plan`、`Read` 三种运行模式。

三种模式不是三个完全独立的 Agent，也不是三个不同的用户任务，而是同一个 Coding Agent 在不同运行阶段下的：

* 行为职责；
* 工具权限；
* 输出形式；
* 完成条件。

模式切换不能改变用户原始任务的语义，只能改变 Agent 当前允许执行的操作。

例如，用户的任务是：

> 重构订单模块。

在不同模式下，任务目标仍然是“重构订单模块”，区别仅在于：

* `Read`：分析订单模块并回答问题；
* `Plan`：调查订单模块并生成重构实施方案；
* `Build`：实际执行订单模块重构。

禁止将模式名称本身当成新的任务目标，例如将 Plan 模式错误理解为“规划如何生成一个方案”。

---

# 2. 统一原则

## 2.1 原始用户请求是任务语义来源

系统必须保留用户原始请求，不能仅用自动提取的目标摘要替代原始请求。

模型生成的任务理解、摘要和计划都是可修正的中间产物，不是绝对事实。

## 2.2 模式只控制执行方式

运行模式主要控制：

* 可以调用哪些工具；
* 是否允许产生副作用；
* 是否可以修改工作区；
* 当前需要生成什么产物；
* 是否需要用户审批。

运行模式不负责重新定义用户目标。

## 2.3 权限必须由运行时强制执行

不能只通过系统提示词限制模型。

即使模型在只读模式下尝试调用写工具，运行时权限系统也必须拒绝该操作。

权限判断至少应覆盖：

* 文件读取；
* 文件修改；
* Shell 命令；
* Git 操作；
* MCP 工具；
* 子智能体派发；
* 工作区外目录访问；
* 外部系统副作用。

## 2.4 模式切换保留会话上下文

模式切换发生在同一个会话和任务上下文中。

切换模式时：

* 保留用户原始请求；
* 保留代码库调查结果；
* 保留已确认的约束；
* 保留已批准的计划；
* 更新当前模式和工具权限；
* 注入最新的模式提醒。

历史消息中提到的旧模式只是历史状态，当前运行模式以最新的运行时状态为准。

---

# 3. Read 模式

## 3.1 模式定位

`Read` 模式用于只读探索、代码理解、问题分析和技术说明。

Read 模式的目标是回答用户当前提出的问题，而不是默认生成实施计划，也不是执行代码修改。

典型场景包括：

* 解释某个模块的实现；
* 分析调用链；
* 查找某个功能对应的代码；
* 审查代码设计；
* 分析错误原因；
* 比较不同实现方式；
* 回答项目架构问题；
* 总结当前代码状态。

## 3.2 行为职责

Read 模式下，Agent 应当：

1. 根据用户问题读取必要的代码和配置；
2. 搜索相关符号、调用关系和测试；
3. 基于实际代码证据回答问题；
4. 明确区分代码事实、合理推断和改进建议；
5. 在信息不足时继续进行只读调查。

Read 模式不要求一定产出执行计划。

如果用户只是询问：

> 这个 checkpoint 模块是怎么工作的？

Agent 应直接解释现有实现，而不是自动输出“第一步修改什么、第二步修改什么”。

## 3.3 工具权限

默认允许：

* 读取文件；
* 搜索文件和符号；
* 查看目录；
* 查看 Git 状态和历史；
* 执行经过允许的只读命令；
* 调用只读 MCP 工具；
* 派发只读探索子智能体。

默认禁止：

* 修改、创建或删除文件；
* 执行格式化、安装、构建等可能修改工作区的命令；
* Git commit、checkout、reset 等状态修改；
* 调用具有外部副作用的工具；
* 通过子智能体绕过当前权限。

## 3.4 输出形式

Read 模式的输出根据用户问题决定，可以是：

* 代码解释；
* 调用链分析；
* 问题定位；
* 架构说明；
* 风险分析；
* 改进建议；
* 文件或符号列表。

Read 模式不应强制使用 Plan 的结构化步骤格式。

## 3.5 完成条件

满足以下条件时可以结束：

* 已直接回答用户问题；
* 结论有足够的代码证据；
* 不再需要额外的只读调查；
* 明确说明仍然存在的不确定信息。

---

# 4. Plan 模式

## 4.1 模式定位

`Plan` 模式用于在不修改项目代码的前提下，调查用户提出的软件工程任务，并形成可供 Build 模式直接执行的实施方案。

Plan 模式不是“规划如何生成方案”，而是：

> 通过实际的只读代码调查，把原本需要执行的代码变更整理成可审批、可执行、可验证的实施方案。

## 4.2 行为职责

Plan 模式下，Agent 应当：

1. 理解用户的原始请求；
2. 使用只读工具调查当前代码实现；
3. 查找相关文件、符号、调用链和测试；
4. 识别约束、影响范围和潜在风险；
5. 设计实际的软件变更方案；
6. 检查方案是否与用户原始请求一致；
7. 输出任务理解和实施方案；
8. 等待用户审批，不直接执行修改。

Plan 模式中的代码库调查是 Agent 当前需要真实执行的工作，不能简单写成最终计划中的抽象步骤。

错误示例：

```text
1. 分析用户需求
2. 查看相关代码
3. 制定重构方案
4. 向用户展示方案
```

这些内容描述的是 Agent 如何生成计划，不是 Build 模式需要执行的代码变更。

正确示例：

```text
1. 将订单只读查询逻辑从 OrderService 提取到 OrderQueryService。
2. 调整 OrderController 和 RefundService 的查询依赖。
3. 保留现有接口参数和返回结构，避免影响外部调用方。
4. 补充订单查询服务单元测试，并运行订单模块回归测试。
```

## 4.3 工具权限

默认允许：

* Read 模式允许的全部只读工具；
* 读取项目结构和相关代码；
* 搜索调用链和依赖；
* 查看测试和配置；
* 运行确定为只读的检查命令；
* 派发只读 Explore 或 Plan 子智能体；
* 写入专门的计划存储区，若项目设计了 Plan Artifact。

默认禁止：

* 修改项目源代码；
* 修改项目配置；
* 创建或删除项目文件；
* 执行会改变工作区的命令；
* 安装依赖；
* 提交代码；
* 通过 MCP 或子智能体绕过只读限制。

如果允许写入计划文件，计划文件必须位于独立的受控区域，不能借此修改项目代码。

## 4.4 计划产物

Plan 模式的最终产物至少应包含：

### 任务理解

说明 Agent 如何理解用户请求，使用户能够发现语义偏差。

### 当前实现

列出调查到的关键文件、符号、调用链和现有约束。

### 目标设计

说明修改完成后的职责划分、数据流或系统行为。

### 实施步骤

描述 Build 模式需要执行的真实代码修改。

每个步骤应尽量包含：

* 修改目标；
* 涉及文件或模块；
* 具体变更；
* 前置依赖；
* 验证方式。

### 影响范围

说明可能受到影响的模块、接口、数据结构和兼容性。

### 风险与待确认项

说明尚未确定的需求、技术风险和需要用户决策的事项。

### 验证方案

说明 Build 完成后需要执行的测试、编译、静态检查或人工验证。

## 4.5 审批语义

用户审批的不只是几个实施步骤，而是：

* Agent 对任务的理解；
* 修改范围；
* 目标设计；
* 实施步骤；
* 风险和验证方式。

计划批准后，应保存为不可变或带版本号的 `ApprovedPlan`。

用户修改需求后，原计划应标记为过期，需要重新生成或修订并再次审批。

## 4.6 完成条件

Plan 模式不能以“已经分析需求”作为完成条件。

进入等待审批状态前，应满足：

* 已完成必要的代码库调查；
* 计划描述实际代码变更，而非元规划过程；
* 计划具有明确的执行顺序；
* 计划包含影响范围和验证方法；
* 任务理解对用户可见；
* 计划已经过基本完整性检查。

---

# 5. Build 模式

## 5.1 模式定位

`Build` 模式用于实际完成用户的软件工程任务。

Build 是默认的执行模式，可以读取代码、修改文件、执行命令、运行测试并根据验证结果继续修复。

如果 Build 是从 Plan 切换而来，应以用户批准的计划作为执行依据。

## 5.2 行为职责

Build 模式下，Agent 应当：

1. 理解用户原始请求；
2. 加载已批准计划，若存在；
3. 按照计划执行代码修改；
4. 根据实际代码情况处理必要的局部调整；
5. 记录计划步骤的执行状态；
6. 运行测试、编译或其他验证；
7. 根据验证失败结果修复或重规划；
8. 只有在最新验证通过后才声明完成。

Build 模式不是机械地逐字执行计划。

如果执行过程中发现：

* 计划依赖的代码已经变化；
* 计划中的文件不存在；
* 实际架构与 Plan 调查结果不一致；
* 修改范围明显超出已批准边界；
* 出现新的高风险操作；

Agent 应暂停相关步骤，更新计划或请求用户确认，而不是强行继续。

## 5.3 工具权限

Build 模式可以根据权限策略开放：

* 文件读取；
* 文件创建、修改和删除；
* Shell 命令；
* 构建和测试；
* 格式化和静态检查；
* Git diff 等版本控制操作；
* MCP 工具；
* 执行型子智能体。

高风险操作仍应单独审批，例如：

* 删除大量文件；
* 修改工作区外文件；
* 安装系统级依赖；
* 修改数据库或远程环境；
* 执行不可逆命令；
* 提交或推送代码；
* 调用生产系统接口。

Build 模式不等于无限权限模式。

## 5.4 计划执行

存在已批准计划时，Build 应维护执行状态，例如：


如果没有经过 Plan 模式，Build 可以直接根据用户请求执行，但复杂任务仍可在内部维护轻量执行步骤。

## 5.5 验证要求

代码修改完成不等于任务完成。

Build 模式必须尽可能进行与任务匹配的验证，包括：

* 编译；
* 单元测试；
* 集成测试；
* 静态检查；
* 类型检查；
* 格式检查；
* 关键功能人工检查；
* Git diff 审查。

如果验证无法运行，应明确说明：

* 哪些验证没有执行；
* 为什么无法执行；
* 当前结论基于什么证据；
* 仍然存在什么风险。

禁止在没有最新验证证据的情况下宣称任务已经完成。

## 5.6 完成条件

Build 模式只有在满足以下条件后才能结束：

* 用户请求对应的修改已经实现；
* 必要的计划步骤已经完成或明确说明偏差；
* 最新验证已经通过，或无法验证的原因已经明确披露；
* 没有未处理的严重错误；
* 最终结果与用户原始请求一致。

---

# 6. 三种模式的核心区别

| 维度       | Read   | Plan      | Build     |
| -------- | ------ | --------- | --------- |
| 核心职责     | 理解和回答  | 调查并设计实施方案 | 实际修改并验证   |
| 是否修改代码   | 否      | 否         | 是         |
| 是否必须生成计划 | 否      | 是         | 否         |
| 是否允许只读探索 | 是      | 是         | 是         |
| 是否需要用户审批 | 通常不需要  | 最终方案需要    | 高风险操作可能需要 |
| 主要产物     | 分析和回答  | 可执行方案     | 代码变更和验证结果 |
| 完成标准     | 问题得到回答 | 方案可直接执行   | 修改完成且经过验证 |

---

# 7. 模式切换规则

## 7.1 Read → Plan

适用于用户从代码理解转向方案设计。

切换后：

* 保留 Read 阶段的调查结果；
* 开始生成正式计划；
* 不允许修改工作区；
* 最终进入等待审批状态。

## 7.2 Read → Build

适用于用户在分析后直接要求修改代码。

切换后：

* 保留已有调查证据；
* 开放经过策略允许的写工具；
* 开始执行修改和验证。

## 7.3 Plan → Build

只能在以下条件下切换：

* Plan 已经生成；
* 用户明确批准；
* 当前批准的计划版本仍然有效；
* 任务约束未发生重大变化。

切换后应向模型明确注入：

```text
当前模式已经从 Plan 切换为 Build。

用户原始请求保持不变。
以下计划已经获得用户批准，可作为执行依据。
你现在可以在权限策略允许的范围内修改代码和执行命令。
```

## 7.4 Build → Plan

出现以下情况时可以回到 Plan：

* 实际代码与计划基础明显不一致；
* 用户要求重新设计；
* 修改范围发生重大变化；
* 需要新的架构决策；
* 当前方案无法继续执行。

重新进入 Plan 后，必须恢复只读限制。修订后的计划需要再次审批。

## 7.5 任意模式 → Read

适用于用户暂停执行，转而询问或审查当前状态。

进入 Read 后不得继续执行尚未完成的修改步骤。

---

# 8. 禁止的错误实现

## 8.1 不得为每种模式创建完全独立的任务语义

错误设计：

```text
Plan 任务：生成计划
Build 任务：编写代码
Read 任务：解释代码
```

正确设计：

```text
用户任务保持不变。
不同模式只是采用不同权限和交付形式处理同一个任务。
```

## 8.2 不得只依赖提示词实现权限控制

即使系统提示词写明“禁止修改文件”，运行时仍必须拦截写工具。

## 8.3 不得使用规则强行提取最终目标

可以生成任务摘要辅助模型，但不能认为摘要一定正确，也不能丢弃用户原始请求。

## 8.4 不得让子智能体绕过父模式权限

子智能体权限必须等于或小于父 Agent 当前权限。

例如 Plan 模式派发的子智能体只能是只读子智能体。

## 8.5 不得把调查过程当成实施计划

“分析需求”“查看代码”“设计方案”是 Plan Agent 当前应执行的工作，不是 Build 阶段的核心实施步骤。

## 8.6 不得在未经批准时自动从 Plan 进入 Build

计划生成完成后必须进入明确的等待审批状态。

普通自然语言回复不能替代正式的状态转换。

---

